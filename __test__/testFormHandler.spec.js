import { handleSubmit } from "../src/client/js/formHandler"

describe("Testing the submit functionality", () => {
    test("Make sure that handleSubmit() function", () => {
           expect(handleSubmit).toBeDefined();
})});
